node_modules/
dist/

# Environment files — .env.staging is safe to commit (test keys only)
.env
.env.prod
.env.test
# Render staging paste file — real staging secrets, never commit
.env.render-staging

# Test artifacts
.seed-state.json
test-results/
playwright-report/
