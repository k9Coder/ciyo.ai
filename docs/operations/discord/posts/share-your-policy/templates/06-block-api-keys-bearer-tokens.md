**Block generic API keys and bearer tokens in prompts**
Payment, messaging, and internal-API keys hide in curl commands and HTTP headers that people paste without reading twice.
`Data types:` API keys, bearer tokens, JWTs, webhook secrets | `Destinations:` ChatGPT, Claude, Gemini | `Action:` block
`[config: pending engineering]`
