**Block database connection strings in AI prompts**
"Why won't this query run?" — pasted with the full `postgres://user:password@host` string, credentials and network location in one line.
`Data types:` Database connection strings (Postgres, MySQL, MongoDB, Redis) | `Destinations:` ChatGPT, Claude, Gemini | `Action:` block
`[config: pending engineering]`
