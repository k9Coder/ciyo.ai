**Block cloud service-account credential files in uploads**
A GCP service-account JSON dragged into an AI chat for "help with auth errors" is a machine identity handed to the internet.
`Data types:` Service-account key files, cloud credential JSON | `Destinations:` ChatGPT, Claude, Gemini (upload) | `Action:` block
`[config: pending engineering]`
