**Block .env file contents in pastes and uploads**
One `.env` file is the whole application's secret inventory — and it's the first thing people share when deployment breaks.
`Data types:` Environment variable files, secrets in KEY=value format | `Destinations:` ChatGPT, Claude, Gemini (paste + upload) | `Action:` block
`[config: pending engineering]`
