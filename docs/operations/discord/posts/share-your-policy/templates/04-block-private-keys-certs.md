**Block private keys and certificates in pastes and uploads**
An SSH or TLS private key pasted for "format debugging" is a credential you can never un-share — rotation is your only exit.
`Data types:` PEM/OpenSSH private keys, PKCS files | `Destinations:` ChatGPT, Claude, Gemini (paste + upload) | `Action:` block
`[config: pending engineering]`
