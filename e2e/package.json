{
  "name": "ciyo-e2e",
  "version": "0.1.0",
  "description": "Cross-service system contract tests",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test --config ../playwright.config.ts --project=cross-service"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "dotenv": "^16.4.5"
  }
}
